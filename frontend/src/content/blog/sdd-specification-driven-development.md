---
title: "SDD：AI写代码最大的问题不是能力，是约束"
description: "规范驱动开发（SDD）如何解决AI编程中的'正确但不对'问题。"
pubDate: 2026-03-30
category: "技术"
tags: ["AI", "SDD", "规范", "开发流程"]
---

## 一个我见过的场景

用户给AI下了一个需求："帮我写一个用户登录功能"。

AI秒出几百行代码。看起来不错？

但仔细一看：
- 用户名长度限制是20个字符（产品要求50个）
- 密码校验规则和安全团队要求不符
- 没考虑手机号登录场景
- 错误提示是英文
- 用了全新的状态管理方案，和项目架构格格不入

**这不是AI能力不够，是约束不够。**

---

## SDD的核心

> **先想清楚再动手，用规范约束行为，用验证确保正确。**

传统AI辅助编码：

```
用户描述需求 → AI写代码 → 用户检查 → 发现不对 → 重来
```

SDD的思路：

```
用户描述需求 → 生成规范文档 → AI按规范实现 → 按规范验证
```

---

## 三层规范体系

得物实战中总结的三层结构：

```
第一层：约束层 → 告诉AI「禁止什么、必须怎样」
第二层：示范层 → 告诉AI「标准产出长什么样」
第三层：视觉层 → 告诉AI「页面应该长什么样」
```

### 为什么需要三层？

**只有约束层时**，AI知道规则但缺乏参考实现，容易在复杂场景下产生符合规则但不符合团队风格的代码。

**加入示范层和视觉层后**，AI可以直接对齐团队的标准产出，减少"虽然合法但不地道"的代码。

---

## 结论

- AI编程最大的问题是**约束不足**，不是能力不足
- SDD通过**先规范后实现**的流程解决问题
- 三层规范体系（约束+示范+视觉）效果最好
- 规范要**具体可执行**，不是模糊的原则
