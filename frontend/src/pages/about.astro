---
import Base from '../layouts/Base.astro';
---

<Base title="关于 · 鱼腹" description="关于鱼腹的写作方向、技术栈和站点理念。">
  <article class="about">
    <section class="about-hero">
      <p class="eyebrow">About This Space</p>
      <h1>不是只放文章的地方，是一个持续生长的个人界面。</h1>
      <p class="lead">
        鱼腹用 Astro 搭底座，但真正想做的不是“再一个博客”，而是把写作、设计和个人表达整合成一个更完整的数字名片。
      </p>
    </section>

    <section class="about-grid">
      <div class="about-card">
        <h2>这个站点在写什么</h2>
        <p>
          这里会记录 AI 工具链、工程方法、开发体验优化，也会留下少量更个人化的观察。内容不会只追求教程感，
          也会保留观点、判断和情绪纹理。
        </p>
      </div>

      <div class="about-card highlight">
        <h2>想要的气质</h2>
        <blockquote>
          生活有温度，技术有骨架。好的个人站点，应该两者都看得见。
        </blockquote>
        <p>所以这个博客会同时在意速度、排版、氛围和表达密度，而不是只把页面做成文章列表。</p>
      </div>

      <div class="about-card">
        <h2>技术栈</h2>
        <ul>
          <li><strong>Astro</strong>：静态优先，页面干净，性能稳。</li>
          <li><strong>Markdown / MDX</strong>：写作简单，迁移成本低。</li>
          <li><strong>Netlify</strong>：部署直接，适合个人内容站。</li>
          <li><strong>GitHub</strong>：版本管理和持续演进的记录面。</li>
        </ul>
      </div>

      <div class="about-card">
        <h2>联系</h2>
        <p>如果你想交流站点设计、Agent 工程、博客系统或者别的想法，可以在 GitHub 上找到我。</p>
        <p><a href="https://github.com/yves960" target="_blank" rel="noreferrer">@yves960</a></p>
      </div>
    </section>
  </article>
</Base>

<style>
  .about {
    max-width: 1040px;
    margin: 0 auto;
    padding: 1rem 0 2rem;
  }

  .about-hero,
  .about-card {
    border: 1px solid rgba(255, 255, 255, 0.09);
    background: linear-gradient(180deg, rgba(9, 17, 31, 0.8), rgba(12, 24, 43, 0.72));
    backdrop-filter: blur(16px);
    box-shadow: 0 18px 60px rgba(0, 0, 0, 0.18);
  }

  .about-hero {
    padding: clamp(2rem, 4vw, 3rem);
    border-radius: 30px;
  }

  .eyebrow {
    margin-bottom: 1rem;
    color: var(--accent-soft);
    letter-spacing: 0.24em;
    text-transform: uppercase;
    font-size: 0.78rem;
  }

  .about-hero h1 {
    max-width: 12ch;
    font-size: clamp(2.8rem, 6vw, 5rem);
    line-height: 0.96;
    letter-spacing: -0.05em;
  }

  .lead {
    max-width: 44rem;
    margin-top: 1.25rem;
    color: var(--text-secondary);
    font-size: 1.05rem;
    line-height: 1.85;
  }

  .about-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.2rem;
    margin-top: 1.2rem;
  }

  .about-card {
    border-radius: 26px;
    padding: 1.5rem;
  }

  .about-card h2 {
    margin-bottom: 0.9rem;
    font-size: 1.4rem;
  }

  .about-card p,
  .about-card li {
    color: var(--text-secondary);
    line-height: 1.8;
  }

  .about-card ul {
    padding-left: 1.2rem;
  }

  .about-card li + li {
    margin-top: 0.35rem;
  }

  .highlight {
    background:
      linear-gradient(180deg, rgba(9, 17, 31, 0.76), rgba(12, 24, 43, 0.72)),
      radial-gradient(circle at top right, rgba(255, 122, 24, 0.18), transparent 35%);
  }

  blockquote {
    margin-bottom: 1rem;
    padding-left: 1rem;
    border-left: 3px solid var(--accent);
    color: #fff1d8;
    font-size: 1.08rem;
    line-height: 1.8;
  }

  a {
    color: var(--accent-soft);
    text-decoration: none;
  }

  a:hover {
    color: #ffe0b0;
  }

  @media (max-width: 760px) {
    .about-grid {
      grid-template-columns: 1fr;
    }

    .about-hero,
    .about-card {
      border-radius: 22px;
    }

    .about-hero h1 {
      max-width: none;
    }
  }
</style>
